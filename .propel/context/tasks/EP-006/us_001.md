---
id: us_001
epic: EP-006
title: "External Assessment Launch API — POST to Provider and Return Test URL"
status: draft
priority: high
story_points: 8
phase: 3
created: 2026-07-22
---

# US-001 — External Assessment Launch API — POST to Provider and Return Test URL

## User Story

**As a** recruiter,  
**I want** to trigger an external online assessment for a candidate by calling the assessment provider's API and receiving a direct test URL to share with the candidate,  
**So that** the assessment step is initiated without the candidate needing to register on the provider platform separately.

## Business Value

Seamless assessment launch from within TalentForge eliminates the manual step of logging into a third-party dashboard and copy-pasting URLs. It also ensures the session is linked to the correct application record from the start.

## Acceptance Criteria

### Scenario 1: Assessment session created and URL returned
**Given** a recruiter clicks "Launch Assessment" for a shortlisted candidate  
**When** the API calls the provider endpoint  
**Then** a session is created with the provider, a test URL and session token are returned, stored in `assessment_sessions`, and the candidate receives an email with the link within 2 minutes.

### Scenario 2: Provider API failure triggers retry
**Given** the assessment provider API returns HTTP 503  
**When** the launch request fails  
**Then** the system retries up to 3 times with 30-second intervals; after 3 failures an alert is sent to the recruiter and the assessment status is set to `"launch_failed"`.

### Scenario 3: Session token stored and linked to application
**Given** the provider returns a session token  
**When** the session is created  
**Then** `assessment_sessions.session_token`, `application_id`, `provider_id`, `launched_at`, and `status = "in_progress"` are all stored correctly.

### Scenario 4: Provider configuration is per-requisition
**Given** two different requisitions use different assessment providers  
**When** an assessment is launched for each  
**Then** each launch calls the correct provider API using the provider configured for that requisition type.

## INVEST Checklist

- [x] Independent — launch API independent of webhook score ingestion
- [x] Negotiable — provider integration details and retry policy are negotiable
- [x] Valuable — eliminates manual out-of-system assessment workflow
- [x] Estimable — 8 SP; external API integration with retry and session storage
- [x] Small — launch only; score ingestion via webhook is US-002
- [x] Testable — session creation, retry behaviour, and per-requisition config are verifiable

## Effort Estimate

**Story Points**: 8 | **T-Shirt Size**: L

## Dependencies

- EP-005 / US-005 (stage prerequisites met)
- EP-DATA / US-001 (`assessment_sessions` table)

## Traceability

- **Epic**: EP-006
- **FR**: FR-044 (assessment launch)
- **TR**: TR-005.4 (third-party assessment provider integration)

## Definition of Done

- [ ] `POST /assessments/launch` endpoint with application ID and provider ID
- [ ] Provider API called; session token and test URL stored
- [ ] Candidate notified via email within 2 min of successful launch
- [ ] 3-retry backoff on provider failure; `launch_failed` status on exhaustion
- [ ] Provider config: stored in `assessment_providers` table, mapped per requisition type
- [ ] Launch event written to `audit_events`
